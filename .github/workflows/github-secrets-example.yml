# GitHub Secrets Example Workflow
#
# This workflow demonstrates using GitHub repository secrets securely in CI/CD.
# Prerequisites:
# - Set required secrets in repository settings (e.g., MY_SECRET)

name: "GitHub Secrets Example"

on:
  workflow_dispatch:

jobs:
  use-github-secrets:
    name: "Use GitHub Secrets"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use a secret in a script
        run: |
          echo "My secret is masked: ${{ secrets.MY_SECRET }}"
          # Add your secret-using logic here


